#ifndef OSAL_WIN32_REGEXDEFS_HXX
#define OSAL_WIN32_REGEXDEFS_HXX

// win32 doesn't have a notion of regular expressions.
//
// we need to rely on whatever common part thinks is right
#include <osal/common/RegexDefs.hxx>

#endif
