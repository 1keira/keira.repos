#ifndef OSAL_WIN32_OSALFSDEFS_HXX
#define OSAL_WIN32_OSALFSDEFS_HXX

#include <common/stdint.h>

typedef intptr_t EntityID;

struct DirFillerHandle { };

struct UserFSRequestHandle { };

struct UserFSEntityHandle { };

struct UserFSHandle { };

#endif // inc. guard
